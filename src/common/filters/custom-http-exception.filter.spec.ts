import { CustomHttpExceptionFilter } from './custom-http-exception.filter';

describe('HttpExceptionFilter', () => {
  it('should be defined', () => {
    expect(CustomHttpExceptionFilter).toBeDefined();
  });
});
