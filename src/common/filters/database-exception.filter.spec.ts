import { DatabaseExceptionFilter } from './database-exception.filter';

describe('DatabaseExceptionFilter', () => {
  it('should be defined', () => {
    expect(DatabaseExceptionFilter).toBeDefined();
  });
});
