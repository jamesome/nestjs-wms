import { PartialResponseInterceptor } from './partial-response.interceptor';

describe('PartialResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new PartialResponseInterceptor()).toBeDefined();
  });
});
